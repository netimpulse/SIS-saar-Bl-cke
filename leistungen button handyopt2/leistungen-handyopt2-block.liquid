{% comment %}
Snippet "Leistungen Handyopt2 Block" – mobiler Content des einzelnen Slides
{% endcomment %}

<article class="service-item service-item-{{ block.id }}">
  <div class="service-item__timeline">
    <div class="service-item__image-wrapper">
      {%- if block.settings.main_image != blank -%}
        <img
          class="service-item__main-image"
          src="{{ block.settings.main_image | image_url: width: 450 }}"
          srcset="
            {{ block.settings.main_image | image_url: width: 180 }} 180w,
            {{ block.settings.main_image | image_url: width: 300 }} 300w,
            {{ block.settings.main_image | image_url: width: 450 }} 450w,
            {{ block.settings.main_image | image_url: width: 600 }} 600w,
            {{ block.settings.main_image | image_url: width: 900 }} 900w
          "
          sizes="(max-width: 749px) 70vw, (max-width: 990px) 45vw, 25vw"
          alt="{{ block.settings.main_image.alt | escape }}"
          loading="lazy"
          decoding="async"
        >
      {%- else -%}
        {{ 'image' | placeholder_svg_tag: 'service-item__main-image placeholder' }}
      {%- endif -%}

      {%- if block.settings.number_icon != blank -%}
        <img
          class="service-item__number-icon"
          src="{{ block.settings.number_icon | image_url: width: 120 }}"
          srcset="
            {{ block.settings.number_icon | image_url: width: 80 }} 80w,
            {{ block.settings.number_icon | image_url: width: 120 }} 120w
          "
          sizes="(max-width: 749px) 64px, 80px"
          alt="{{ block.settings.number_icon.alt | default: '' | escape }}"
          loading="lazy"
          decoding="async"
          {%- if block.settings.number_icon.alt == blank -%}aria-hidden="true" role="presentation"{%- endif -%}
        >
      {%- endif -%}
    </div>
  </div>

  <div class="service-item__content">
    {%- if block.settings.title != blank -%}
      <h3 class="service-item__title">{{ block.settings.title }}</h3>
    {%- endif -%}
    {%- if block.settings.subtitle != blank -%}
      <h4 class="service-item__subtitle">{{ block.settings.subtitle }}</h4>
    {%- endif -%}
    {%- if block.settings.text != blank -%}
      <div class="service-item__text">{{ block.settings.text }}</div>
    {%- endif -%}
  </div>
</article>
